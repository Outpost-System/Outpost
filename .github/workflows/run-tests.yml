name: run-tests

on: 
  - push

jobs:
  tests:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/outpost-system/busted:latest
    steps:
    - uses: actions/checkout@v1
    - name: Run luacheck
      run: luacheck .
    - name: Run busted tests
      run: busted